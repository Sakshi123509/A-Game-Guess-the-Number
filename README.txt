🎯 Guess The Number Game

This is a simple and fun number guessing game built using **HTML**, **CSS**, and **JavaScript**. The goal is to guess the randomly generated number within a specific range. With each guess, the player receives helpful hints — whether the guess is too high, too low, or exactly right!

🔧 Technologies Used

- **HTML** – for building the structure of the webpage.
- **CSS** – for styling and layout.
- **JavaScript** – for game functionality and logic.

 💡 JavaScript Concepts Used

- **Random Number Generation** using `Math.random()` and `Math.floor()`
- **Handling User Input** via `addEventListener()` and form controls
- **DOM Manipulation** using `document.querySelector()` to update content dynamically
- **Conditional Logic** to compare the user's guess and give feedback
- **Game Reset Functionality** to play again without refreshing the page

 🕹 Features

- Random number is generated between a fixed range (e.g., 1–100)
- Real-time feedback on guesses:
  - 🔼 Too high
  - 🔽 Too low
  - ✅ Correct guess!
- Score tracking (number of attempts)
- "Play Again" or "Restart Game" button to reset the game state
- Responsive and user-friendly interface

 🚀 How to Run the Game

1. Clone or download the repository.
2. Open the `index.html` file in your browser.
3. Start guessing the number and enjoy!

```bash
git clone https://github.com/your-username/guess-the-number.git
cd guess-the-number
